<div class="container-fluid">
  <div class="row-fluid">
    <div class="span8">
      <div class="well">
        <h1>Topic #<%=@topic.id%></h1>
        <p><%=@topic.content%></p>
      </div>
      <h1>Essay #<%=@essay.id%></h1>
      <div class="well">
        <p><%=simple_format(@essay.content)%></p>
        <p class="muted">- <%=@essay.username%>, <%=@essay.created_at.to_formatted_s(:long_ordinal)%></p>
      </div>
      <a href="/study/comment/<%=@essay.id%>" class="btn btn-block btn-info btn-large">Comment</a>
      <h1>Other essayes in topic #<%=@topic.id%></h1>
      <table class="table">
        <thead>
          <tr>
            <th>#</th>
            <th>Summary</th>
            <th>Author</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
				<% @essays.each_with_index do |l, index| %>
          <tr>
            <td><%=index+1%></td>
            <td><p><%=truncate(l.summary, length: 50)%></p></td>
            <td><%=l.username%></td>
            <td>
              <a href="/study/read/<%=l.id%>" class="btn btn-success btn-small">Read</a> 
            </td>
          </tr>
				<% end %>
        </tbody>
      </table>
    </div>
    <div class="span4">
      <div class="well">
				<%= render "dic" %>
      </div>
    </div>
  </div>
</div>
